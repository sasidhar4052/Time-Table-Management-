<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Time Table Management System</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body Styling */
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #74c7f0, #bb86fc);
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Container Styling */
        .container {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.15);
            width: 380px;
            padding: 40px;
            text-align: center;
        }

        /* Heading Styling */
        .container h1 {
            font-size: 26px;
            font-weight: 700;
            color: #333;
            margin-bottom: 25px;
        }

        /* Login Form */
        .login-form h2 {
            font-size: 20px;
            color: #5b8def;
            margin-bottom: 25px;
            font-weight: 600;
        }

        /* Input Group Styling */
        .input-group {
            position: relative;
            margin-bottom: 20px;
        }

        .input-group input[type="text"],
        .input-group input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            transition: border-color 0.3s;
            background-color: #f9fafb;
            color: #333;
        }

        .input-group input:focus {
            border-color: #5b8def;
            outline: none;
            background-color: #ffffff;
        }

        /* Button Styling */
        #loginBtn {
            width: 100%;
            padding: 12px;
            font-size: 16px;
            color: #fff;
            background-color: #5b8def;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            font-weight: 600;
        }

        #loginBtn:hover {
            background-color: #3b6dbf;
            transform: translateY(-2px);
        }

        #loginBtn:active {
            background-color: #5b8def;
            transform: translateY(0);
        }

        /* Admin Login Link */
        .admin-link {
            display: block;
            margin-top: 20px;
            font-size: 14px;
            color: #5b8def;
            text-decoration: none;
            transition: color 0.3s;
        }

        .admin-link:hover {
            color: #3b6dbf;
        }

        /* Responsive Design */
        @media (max-width: 420px) {
            .container {
                width: 90%;
                padding: 25px;
            }

            .container h1 {
                font-size: 22px;
            }

            .login-form h2 {
                font-size: 18px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Time Table Management System</h1>
        <form class="login-form" action="/admin/login" method="POST">
            <h2>Admin Login</h2>
            <div class="input-group">
                <input type="text" id="username" name="username" placeholder="Username" required>
            </div>
            <div class="input-group">
                <input type="password" id="password" name="password" placeholder="Password" required>
            </div>
            <button type="submit" id="loginBtn">Login</button>
            <a href="/user/login" class="admin-link">User Login</a>
        </form>
    </div>

</body>
</html>
